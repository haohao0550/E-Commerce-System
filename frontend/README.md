# Mini E-Commerce Frontend

Next.js frontend for the mini e-commerce backend.

## Run

```bash
npm install
npm run dev
```

Default app URL:

```txt
http://localhost:3000
```

Backend API URL is configured with:

```txt
NEXT_PUBLIC_API_BASE_URL=http://localhost:8080/api/v1
```
